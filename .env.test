# Test Environment Configuration
NODE_ENV=test
PORT=3000

# Test Database Configuration
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/ticket_platform_test
REDIS_URL=redis://localhost:6379/1

# Test JWT Configuration
JWT_SECRET=test-jwt-secret-key
JWT_REFRESH_SECRET=test-refresh-secret-key
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Test Stripe Configuration (use test keys)
STRIPE_SECRET_KEY=sk_test_test_key
STRIPE_WEBHOOK_SECRET=whsec_test_webhook

# Test AWS Configuration
AWS_ACCESS_KEY_ID=test_access_key
AWS_SECRET_ACCESS_KEY=test_secret_key
AWS_REGION=us-east-1
AWS_S3_BUCKET=test-bucket

# Test Email Configuration
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test@example.com
SMTP_PASS=test_password

# Test Rate Limiting (more lenient for tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000